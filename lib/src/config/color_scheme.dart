import 'package:flutter/material.dart';

class AppColors {
    // Brand
  static const Color primary = Color(0xFF2F6F63);
  static const Color primaryLight = Color(0xFF6FA89A);
  static const Color secondary = Color(0xFFA7CFC2);
  static const Color background = Color(0xFFE9F3EF);

  // Text
  static const Color textPrimary = Color(0xFF1F2933);
  static const Color textSecondary = Color(0xFF6B7280);

  // UI
  static const Color divider = Color(0xFFD1D5DB);
  static const Color disabled = Color(0xFF9CA3AF);

  // States
  static const Color success = primary;
  static const Color pending = disabled;
  static const Color error = Color(0xFFD14343);
  static const Color warning = Color(0xFFF2A900);

  const AppColors._();
}
